/**
 * The stored (or pending) attachment DTO, mirroring the PHP `AttachmentData`
 * class via the generated ambient types so the frontend stays in lockstep with
 * the backend shape.
 */
export type AttachmentData = App.Data.AttachmentData;
